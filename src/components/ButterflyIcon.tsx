import { PiButterfly } from "react-icons/pi";
import type { IconBaseProps } from "react-icons";

export default function ButterflyIcon(props: IconBaseProps) {
  return <PiButterfly aria-hidden {...props} />;
}
